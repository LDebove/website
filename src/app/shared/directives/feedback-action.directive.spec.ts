import { FeedbackActionDirective } from './feedback-action.directive';

describe('FeedbackActionDirective', () => {
  it('should create an instance', () => {
    const directive = new FeedbackActionDirective();
    expect(directive).toBeTruthy();
  });
});
