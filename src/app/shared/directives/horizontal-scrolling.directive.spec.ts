import { HorizontalScrollingDirective } from './horizontal-scrolling.directive';

describe('HorizontalScrollingDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalScrollingDirective();
    expect(directive).toBeTruthy();
  });
});
